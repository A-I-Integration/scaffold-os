// ============================================================ // lib/calculations/cad-engine.ts // SCAFFOLD OS – CAD Geometrie-Engine // ============================================================ // Berechnet aus Gebäude-Parametern und Gerüstsystem-Daten // die vollständige 3D-Geometrie, Feldaufteilung, Lagen und // Bauteil-Positionen. // ============================================================ import { GeruestSystem, findeSystem } from './geruest-systeme'; import { ScaffoldInput, MaterialItem, KIAnalysis } from '@/types/scaffold'; import { calculateScaffoldMaterial } from './scaffold-engine'; // --- TYPEN --- export interface BuildingParams { lengthM: number; widthM: number; heightM: number; eavesHeightM: number; roofHeightM: number; roofForm: 'flachdach' | 'satteldach' | 'walmdach' | 'pultdach' | 'mansardendach' | 'kein'; floors: number; floorHeightsM: number[]; windowCount: number; doorCount: number; balconyCount: number; overhangM: number; setbackM: number; } export interface ScaffoldField { id: string; index: number; lengthM: number; widthM: number; positionX: number; positionY: number; // Höhe der unteren Kante positionZ: number; side: 'front' | 'back' | 'left' | 'right'; levelIndex: number; } export interface ScaffoldLevel { id: string; index: number; heightM: number; // Höhe der Lage von Fußpunkt bottomY: number; topY: number; fields: ScaffoldField[]; } export interface ScaffoldAnchor { id: string; positionX: number; positionY: number; positionZ: number; side: 'front' | 'back' | 'left' | 'right'; type: string; } export interface ScaffoldComponent3D { id: string; type: 'frame' | 'deck' | 'railing' | 'diagonal' | 'footplate' | 'coupling' | 'anchor' | 'console' | 'stair' | 'net' | 'board'; articleNumber: string; name: string; position: [number, number, number]; rotation: [number, number, number]; scale: [number, number, number]; color: string; fieldId?: string; levelId?: string; } export interface CADModel { building: BuildingParams; system: GeruestSystem | null; fields: ScaffoldField[]; levels: ScaffoldLevel[]; anchors: ScaffoldAnchor[]; components3D: ScaffoldComponent3D[]; totalLengthM: number; totalHeightM: number; totalAreaM2: number; fieldCount: number; levelCount: number; warnings: CADWarning[]; } export interface CADWarning { type: 'error' | 'warning' | 'info'; code: string; message: string; fieldId?: string; levelId?: string; } // --- AUTOMATISCHE FELDAUFEILUNG --- export function calculateFieldDivision( buildingLengthM: number, system: GeruestSystem, preferredFieldLength?: number ): { fields: number; fieldLengthM: number; remainderM: number; distribution: number[] } { const available = system.feldlangenM; const target = preferredFieldLength || system.standardFeldlangeM; // Beste Feldlänge finden const bestLength = available.reduce((best, curr) => { const bestFields = Math.ceil(buildingLengthM / best); const currFields = Math.ceil(buildingLengthM / curr); const bestRemainder = Math.abs(buildingLengthM - bestFields * best); const currRemainder = Math.abs(buildingLengthM - currFields * curr); // Priorität: weniger Felder, dann kleinerer Rest if (currFields < bestFields) return curr; if (currFields > bestFields) return best; return currRemainder < bestRemainder ? curr : best; }, target); const fields = Math.max(1, Math.ceil(buildingLengthM / bestLength)); const actualLength = fields * bestLength; const remainder = actualLength - buildingLengthM; // Gleichmäßige Verteilung des Rests const distribution: number[] = []; for (let i = 0; i < fields; i++) { distribution.push(bestLength); } // Letztes Feld ggf. verkürzen (nur wenn Rest > 10cm) if (remainder > 0.1 && fields > 1) { distribution[fields - 1] = parseFloat((bestLength - remainder).toFixed(2)); } return { fields, fieldLengthM: bestLength, remainderM: remainder, distribution }; } // --- AUTOMATISCHE HÖHENAUTEILUNG --- export function calculateLevels( buildingHeightM: number, system: GeruestSystem, groundOffsetM: number = 0 ): { levels: number; levelHeightM: number; levelsData: { index: number; bottomY: number; topY: number }[] } { const rasterH = system.rasterHoeheM; const levels = Math.max(1, Math.ceil(buildingHeightM / rasterH)); const levelsData: { index: number; bottomY: number; topY: number }[] = []; for (let i = 0; i < levels; i++) { const bottomY = groundOffsetM + i * rasterH; const topY = bottomY + rasterH; levelsData.push({ index: i, bottomY: parseFloat(bottomY.toFixed(2)), topY: parseFloat(topY.toFixed(2)), }); } return { levels, levelHeightM: rasterH, levelsData }; } // --- 3D-BAUTEILE GENERIEREN --- export function generateScaffoldComponents( model: CADModel ): ScaffoldComponent3D[] { const components: ScaffoldComponent3D[] = []; const { system, fields, levels } = model; if (!system) return components; const frameWidth = 0.073; // 7,3 cm Rahmenbreite const frameDepth = 0.04; const deckDepth = 0.04; const railingHeight = 1.0; const colorFrame = '#3b82f6'; const colorDeck = '#f59e0b'; const colorRailing = '#ef4444'; const colorDiagonal = '#8b5cf6'; const colorFoot = '#6b7280'; const colorAnchor = '#10b981'; // Für jedes Feld und jede Lage Bauteile generieren fields.forEach((field) => { const { positionX, positionY, positionZ, lengthM, widthM, levelIndex, side } = field; const level = levels.find((l) => l.index === levelIndex); if (!level) return; const yBottom = level.bottomY; const yTop = level.topY; const levelH = level.heightM; // --- RAHMEN (links + rechts) --- components.push({ id: `frame-${field.id}-left`, type: 'frame', articleNumber: getFrameArticle(lengthM), name: `Rahmen ${lengthM}m`, position: [positionX - lengthM / 2 + 0.02, yBottom + levelH / 2, positionZ], rotation: [0, 0, 0], scale: [frameWidth, levelH, frameDepth], color: colorFrame, fieldId: field.id, levelId: level.id, }); components.push({ id: `frame-${field.id}-right`, type: 'frame', articleNumber: getFrameArticle(lengthM), name: `Rahmen ${lengthM}m`, position: [positionX + lengthM / 2 - 0.02, yBottom + levelH / 2, positionZ], rotation: [0, 0, 0], scale: [frameWidth, levelH, frameDepth], color: colorFrame, fieldId: field.id, levelId: level.id, }); // --- QUERRIEGEL (oben + unten) --- components.push({ id: `rail-${field.id}-bottom`, type: 'frame', articleNumber: 'QR-001', name: 'Querriegel', position: [positionX, yBottom + 0.05, positionZ], rotation: [0, 0, 0], scale: [lengthM, 0.04, frameDepth], color: colorFrame, fieldId: field.id, levelId: level.id, }); components.push({ id: `rail-${field.id}-top`, type: 'frame', articleNumber: 'QR-001', name: 'Querriegel', position: [positionX, yTop - 0.05, positionZ], rotation: [0, 0, 0], scale: [lengthM, 0.04, frameDepth], color: colorFrame, fieldId: field.id, levelId: level.id, }); // --- ARBEITSBÜHNE --- components.push({ id: `deck-${field.id}`, type: 'deck', articleNumber: getDeckArticle(lengthM), name: `Arbeitsbühne ${lengthM}m`, position: [positionX, yTop, positionZ + widthM / 2 - 0.02], rotation: [-Math.PI / 2, 0, 0], scale: [lengthM - 0.05, widthM - 0.05, deckDepth], color: colorDeck, fieldId: field.id, levelId: level.id, }); // --- GELÄNDER (oben, nur in oberster Lage oder alle?) --- // Geländer in JEDER Lage außer Boden components.push({ id: `railing-${field.id}-top`, type: 'railing', articleNumber: getRailingArticle(lengthM), name: `Geländer ${lengthM}m`, position: [positionX, yTop, positionZ + widthM / 2 + 0.02], rotation: [0, 0, 0], scale: [lengthM, railingHeight, 0.04], color: colorRailing, fieldId: field.id, levelId: level.id, }); // --- DIAGONALEN (jede 2. Lage, alternierend) --- if (levelIndex % 2 === 0) { components.push({ id: `diagonal-${field.id}`, type: 'diagonal', articleNumber: getDiagonalArticle(lengthM), name: `Diagonale ${lengthM}m`, position: [positionX, yBottom + levelH / 2, positionZ], rotation: [0, 0, Math.atan2(levelH, lengthM)], scale: [0.03, Math.sqrt(lengthM * lengthM + levelH * levelH), 0.03], color: colorDiagonal, fieldId: field.id, levelId: level.id, }); } // --- FUßPLATTEN (nur in unterster Lage) --- if (levelIndex === 0) { components.push({ id: `foot-${field.id}-left`, type: 'footplate', articleNumber: 'FP-001', name: 'Fußplatte', position: [positionX - lengthM / 2 + 0.02, yBottom - 0.02, positionZ], rotation: [0, 0, 0], scale: [0.15, 0.04, 0.15], color: colorFoot, fieldId: field.id, levelId: level.id, }); components.push({ id: `foot-${field.id}-right`, type: 'footplate', articleNumber: 'FP-001', name: 'Fußplatte', position: [positionX + lengthM / 2 - 0.02, yBottom - 0.02, positionZ], rotation: [0, 0, 0], scale: [0.15, 0.04, 0.15], color: colorFoot, fieldId: field.id, levelId: level.id, }); } }); // --- VERANKERUNGEN --- model.anchors.forEach((anchor) => { components.push({ id: `anchor-${anchor.id}`, type: 'anchor', articleNumber: 'AN-001', name: 'Fassadenanker', position: [anchor.positionX, anchor.positionY, anchor.positionZ], rotation: [0, 0, 0], scale: [0.08, 0.08, 0.3], color: colorAnchor, }); }); return components; } // --- HILFSFUNKTIONEN: ARTIKEL-AUSWAHL --- function getFrameArticle(fieldLengthM: number): string { if (fieldLengthM >= 3.0) return 'RA-003'; if (fieldLengthM >= 2.5) return 'RA-002'; return 'RA-001'; } function getDeckArticle(fieldLengthM: number): string { if (fieldLengthM >= 3.0) return 'AB-003'; if (fieldLengthM >= 2.5) return 'AB-002'; return 'AB-001'; } function getRailingArticle(fieldLengthM: number): string { if (fieldLengthM >= 3.0) return 'GE-003'; if (fieldLengthM >= 2.5) return 'GE-002'; return 'GE-001'; } function getDiagonalArticle(fieldLengthM: number): string { if (fieldLengthM >= 3.0) return 'DI-003'; if (fieldLengthM >= 2.5) return 'DI-002'; return 'DI-001'; } // --- HAUPTFUNKTION: KOMPLETTES CAD-MODELL ERZEUGEN --- export function generateCADModel( building: BuildingParams, systemId: string, scaffoldWidthM: number = 0.73, distanceToBuildingM: number = 0.3 ): CADModel { const system = findeSystem(systemId); const warnings: CADWarning[] = []; // Gebäude-Plausibilität if (building.lengthM <= 0) warnings.push({ type: 'error', code: 'BUILDING_LENGTH_ZERO', message: 'Gebäudelänge muss größer als 0 sein.' }); if (building.heightM <= 0) warnings.push({ type: 'error', code: 'BUILDING_HEIGHT_ZERO', message: 'Gebäudehöhe muss größer als 0 sein.' }); if (building.heightM > 40) warnings.push({ type: 'warning', code: 'HEIGHT_VERY_HIGH', message: 'Gebäudehöhe > 40m – Statik prüfen lassen!' }); // Feldaufteilung const fieldDiv = system ? calculateFieldDivision(building.lengthM, system) : { fields: 1, fieldLengthM: 2.07, remainderM: 0, distribution: [2.07] }; // Höhenaufteilung const levelCalc = system ? calculateLevels(building.heightM, system) : { levels: 1, levelHeightM: 2.0, levelsData: [{ index: 0, bottomY: 0, topY: 2.0 }] }; // Felder erzeugen const fields: ScaffoldField[] = []; const levels: ScaffoldLevel[] = []; // Pro Seite (front, back, left, right) – hier zuerst nur Vorderseite const sides: ('front' | 'back' | 'left' | 'right')[] = ['front']; // TODO: Mehrseitige Gerüste sides.forEach((side) => { const zOffset = side === 'front' ? distanceToBuildingM : side === 'back' ? -distanceToBuildingM - scaffoldWidthM : 0; levelCalc.levelsData.forEach((levelData) => { const levelId = `level-${side}-${levelData.index}`; const levelFields: ScaffoldField[] = []; fieldDiv.distribution.forEach((fieldLength, fieldIndex) => { const fieldId = `field-${side}-${levelData.index}-${fieldIndex}`; const totalLength = fieldDiv.distribution.reduce((a, b) => a + b, 0); const xPos = fieldIndex * fieldLength - totalLength / 2 + fieldLength / 2; const field: ScaffoldField = { id: fieldId, index: fieldIndex, lengthM: fieldLength, widthM: scaffoldWidthM, positionX: parseFloat(xPos.toFixed(3)), positionY: levelData.bottomY, positionZ: zOffset, side, levelIndex: levelData.index, }; fields.push(field); levelFields.push(field); }); levels.push({ id: levelId, index: levelData.index, heightM: levelCalc.levelHeightM, bottomY: levelData.bottomY, topY: levelData.topY, fields: levelFields, }); }); }); // Verankerungen (vereinfacht: alle 2 Felder, ab 3. Lage) const anchors: ScaffoldAnchor[] = []; if (building.heightM > 6) { const anchorLevels = Math.floor(levelCalc.levelsData.length / 2); for (let al = 0; al < anchorLevels; al++) { const yAnchor = (al * 2 + 2) * levelCalc.levelHeightM; for (let f = 0; f < fieldDiv.fields; f += 2) { const totalLength = fieldDiv.distribution.reduce((a, b) => a + b, 0); const xPos = f * fieldDiv.fieldLengthM - totalLength / 2 + fieldDiv.fieldLengthM / 2; anchors.push({ id: `anchor-${al}-${f}`, positionX: xPos, positionY: yAnchor, positionZ: distanceToBuildingM + scaffoldWidthM / 2, side: 'front', type: 'fassadenanker', }); } } } // Vorläufiges Modell für Bauteil-Generierung const preliminaryModel: CADModel = { building, system, fields, levels, anchors, components3D: [], totalLengthM: fieldDiv.fields * fieldDiv.fieldLengthM, totalHeightM: building.heightM, totalAreaM2: building.lengthM * building.heightM, fieldCount: fieldDiv.fields, levelCount: levelCalc.levels, warnings, }; // 3D-Bauteile generieren const components3D = generateScaffoldComponents(preliminaryModel); return { ...preliminaryModel, components3D, }; } // --- STÜCKLISTE AUS CAD-MODELL --- export function generateBillOfMaterials(model: CADModel): MaterialItem[] { const counts: Record<string, { name: string; category: string; quantity: number; unit: string; unitPrice: number; weightKg: number; articleNumber: string }> = {}; model.components3D.forEach((comp) => { const key = comp.articleNumber; if (!counts[key]) { counts[key] = { name: comp.name, category: getCategoryFromType(comp.type), quantity: 0, unit: 'Stk', unitPrice: getUnitPrice(key), weightKg: getWeightKg(key), articleNumber: key, }; } counts[key].quantity += 1; }); return Object.values(counts).map((item) => ({ ...item, totalPrice: Math.round(item.quantity * item.unitPrice * 100) / 100, riskLevel: 'low', aiRecommendation: getRecommendation(item.articleNumber), })); } function getCategoryFromType(type: ScaffoldComponent3D['type']): string { const map: Record<string, string> = { frame: 'Rahmen', deck: 'Belag', railing: 'Geländer', diagonal: 'Diagonalen', footplate: 'Fundamente', coupling: 'Kupplungen', anchor: 'Anker', console: 'Konsolen', stair: 'Treppen', net: 'Sicherheit', board: 'Bordbretter', }; return map[type] || 'Sonstiges'; } function getUnitPrice(articleNumber: string): number { const prices: Record<string, number> = { 'RA-001': 45, 'RA-002': 52, 'RA-003': 58, 'AB-001': 85, 'AB-002': 98, 'AB-003': 112, 'DI-001': 28, 'DI-002': 32, 'DI-003': 36, 'GE-001': 35, 'GE-002': 40, 'GE-003': 45, 'FP-001': 18, 'KU-001': 4.5, 'AN-001': 15, 'QR-001': 12, }; return prices[articleNumber] || 10; } function getWeightKg(articleNumber: string): number { const weights: Record<string, number> = { 'RA-001': 12.5, 'RA-002': 15.2, 'RA-003': 18.0, 'AB-001': 22, 'AB-002': 26, 'AB-003': 31, 'DI-001': 8.5, 'DI-002': 9.8, 'DI-003': 11.2, 'GE-001': 7.2, 'GE-002': 8.5, 'GE-003': 9.8, 'FP-001': 5.5, 'KU-001': 0.8, 'AN-001': 2.5, 'QR-001': 3.5, }; return weights[articleNumber] || 5; } function getRecommendation(articleNumber: string): string { const recs: Record<string, string> = { 'RA-001': 'Standard-Rahmen für Feldlänge 2,07 m', 'RA-002': 'Für breitere Felder oder Hindernisumgehung', 'RA-003': 'Für große Feldlängen oder breite Durchgänge', 'AB-001': 'Standard-Arbeitsbühne', 'AB-002': 'Für 2,50 m Feldlänge', 'AB-003': 'Für 3,00 m Feldlänge', 'DI-001': 'Stabilisierung je Feld', 'DI-002': 'Stabilisierung für 2,50 m Felder', 'DI-003': 'Stabilisierung für 3,00 m Felder', 'GE-001': 'Brüstungsgeländer je Ebene', 'GE-002': 'Brüstungsgeländer für 2,50 m Felder', 'GE-003': 'Brüstungsgeländer für 3,00 m Felder', 'FP-001': 'Grundplatte je Standfuß', 'KU-001': 'Verbindung von Rahmen und Diagonalen', 'AN-001': 'Standard-Fassadenanker', 'QR-001': 'Querriegel für Rahmenverbindung', }; return recs[articleNumber] || 'Standard-Bauteil'; } // --- KOLLISIONSERKENNUNG (vereinfacht) --- export function checkCollisions(model: CADModel): CADWarning[] { const warnings: CADWarning[] = []; // Gerüst überschneidet sich mit Gebäude? if (model.system && model.building.widthM > 0) { const scaffoldDepth = model.system.rahmenBreitenM[0] || 0.73; const distanceToBuilding = 0.3; if (scaffoldDepth + distanceToBuilding > model.building.widthM / 2) { warnings.push({ type: 'warning', code: 'SCAFFOLD_TOO_DEEP', message: 'Gerüsttiefe überschreitet Gebäudebreite – bitte prüfen.', }); } } // Feldaufteilung passt nicht? if (model.fieldCount > 0 && model.building.lengthM > 0) { const actualLength = model.fields.reduce((sum, f) => sum + f.lengthM, 0) / model.levelCount; const diff = Math.abs(actualLength - model.building.lengthM); if (diff > model.fields[0]?.lengthM || 0) { warnings.push({ type: 'warning', code: 'FIELD_MISMATCH', message: `Feldaufteilung (${actualLength.toFixed(2)}m) weicht stark von Gebäudelänge (${model.building.lengthM}m) ab.`, }); } } return warnings; } // --- 2D-PROJEKTIONEN --- export interface Projection2D { type: 'grundriss' | 'ansicht' | 'schnitt'; viewBox: { minX: number; minY: number; width: number; height: number }; elements: SVGElement2D[]; dimensions: Dimension2D[]; } export interface SVGElement2D { id: string; type: 'rect' | 'line' | 'circle' | 'text' | 'path'; x: number; y: number; width?: number; height?: number; x2?: number; y2?: number; r?: number; d?: string; text?: string; stroke?: string; fill?: string; strokeWidth?: number; } export interface Dimension2D { id: string; fromX: number; fromY: number; toX: number; toY: number; value: string; label: string; offsetX?: number; offsetY?: number; } export function generateGrundriss(model: CADModel): Projection2D { const { building, fields } = model; const margin = 2; const width = building.lengthM + margin * 2; const depth = building.widthM + margin * 2 || 10; const elements: SVGElement2D[] = []; const dimensions: Dimension2D[] = []; // Gebäude (vereinfacht als Rechteck) elements.push({ id: 'building', type: 'rect', x: 0, y: 0, width: building.lengthM, height: building.widthM || 6, fill: '#e2e8f0', stroke: '#475569', strokeWidth: 2, }); // Gerüstfelder const scaffoldWidth = model.system?.rahmenBreitenM[0] || 0.73; fields.filter((f) => f.levelIndex === 0).forEach((field) => { elements.push({ id: `field-${field.id}`, type: 'rect', x: field.positionX - field.lengthM / 2, y: field.positionZ, width: field.lengthM, height: scaffoldWidth, fill: 'none', stroke: '#3b82f6', strokeWidth: 1.5, }); }); // Bemaßung dimensions.push({ id: 'dim-length', fromX: 0, fromY: (building.widthM || 6) + 0.5, toX: building.lengthM, toY: (building.widthM || 6) + 0.5, value: `${building.lengthM.toFixed(2)} m`, label: 'Gebäudelänge', offsetY: 0.5, }); return { type: 'grundriss', viewBox: { minX: -margin, minY: -margin, width, height: depth }, elements, dimensions, }; } export function generateAnsicht(model: CADModel): Projection2D { const { building, fields, levels } = model; const margin = 2; const width = building.lengthM + margin * 2; const height = building.heightM + margin * 2; const elements: SVGElement2D[] = []; const dimensions: Dimension2D[] = []; // Gebäude elements.push({ id: 'building-face', type: 'rect', x: 0, y: 0, width: building.lengthM, height: building.heightM, fill: '#f1f5f9', stroke: '#64748b', strokeWidth: 1, }); // Dach (vereinfacht) if (building.roofForm !== 'kein') { elements.push({ id: 'roof', type: 'path', x: 0, y: 0, d: `M 0,${building.heightM} L ${building.lengthM / 2},${building.heightM + 1.5} L ${building.lengthM},${building.heightM} Z`, fill: '#94a3b8', stroke: '#475569', strokeWidth: 1, }); } // Gerüstfelder und Lagen levels.forEach((level) => { level.fields.forEach((field) => { elements.push({ id: `scaffold-${field.id}`, type: 'rect', x: field.positionX - field.lengthM / 2, y: level.bottomY, width: field.lengthM, height: level.heightM, fill: 'none', stroke: '#3b82f6', strokeWidth: 1.5, }); }); }); // Bemaßungen dimensions.push({ id: 'dim-total-height', fromX: -0.5, fromY: 0, toX: -0.5, toY: building.heightM, value: `${building.heightM.toFixed(2)} m`, label: 'Gebäudehöhe', offsetX: -0.8, }); dimensions.push({ id: 'dim-scaffold-height', fromX: building.lengthM + 0.5, fromY: 0, toX: building.lengthM + 0.5, toY: levels[levels.length - 1]?.topY || building.heightM, value: `${(levels[levels.length - 1]?.topY || building.heightM).toFixed(2)} m`, label: 'Gerüsthöhe', offsetX: 0.8, }); return { type: 'ansicht', viewBox: { minX: -margin, minY: -margin, width, height }, elements, dimensions, }; }