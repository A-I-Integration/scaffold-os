'use client';;import { useRef, useMemo, useState } from 'react';;import { Canvas, useThree } from '@react-three/fiber';;import { OrbitControls, Grid, Text } from '@react-three/drei';;import * as THREE from 'three';;import { CADModel, ScaffoldComponent3D } from '@/lib/calculations/cad-engine';;interface Props {;  model: CADModel;;  showBuilding: boolean;;  showScaffold: boolean;;  showDimensions: boolean;;  selectedComponent: string | null;;  onSelectComponent: (id: string | null) => void;;  visibleTypes: Record<string, boolean>;;  viewMode: 'perspective' | 'front' | 'back' | 'left' | 'right' | 'top' | 'bottom';;};function ComponentMesh({ comp, isSelected, onClick, visible }: {;  comp: ScaffoldComponent3D;;  isSelected: boolean;;  onClick: () => void;;  visible: boolean;;}) {;  const [hovered, setHovered] = useState(false);;  if (!visible) return null;;  const emissiveIntensity = isSelected ? 0.5 : hovered ? 0.2 : 0;;  const opacity = isSelected ? 1 : hovered ? 0.9 : 0.85;;  const isTransparent = comp.type === 'net' || comp.type === 'safety_net';;  const isMetal = comp.type === 'frame' || comp.type === 'railing' || comp.type === 'diagonal' || comp.type === 'console';;  const isWood = comp.type === 'board';;  return (;    <mesh;      position={comp.position};      rotation={comp.rotation};      scale={comp.scale};      onClick={(e) => { e.stopPropagation(); onClick(); }};      onPointerOver={(e) => { e.stopPropagation(); setHovered(true); document.body.style.cursor = 'pointer'; }};      onPointerOut={() => { setHovered(false); document.body.style.cursor = 'default'; }};    >;      {comp.type === 'frame' && <boxGeometry args={[1, 1, 1]} />};      {comp.type === 'deck' && <boxGeometry args={[1, 1, 0.02]} />};      {comp.type === 'railing' && <boxGeometry args={[1, 1, 0.04]} />};      {comp.type === 'diagonal' && <boxGeometry args={[0.03, 1, 0.03]} />};      {comp.type === 'footplate' && <cylinderGeometry args={[0.075, 0.075, 0.04, 16]} />};      {comp.type === 'coupling' && <sphereGeometry args={[0.04, 12, 12]} />};      {comp.type === 'anchor' && <cylinderGeometry args={[0.04, 0.04, 0.3, 8]} />};      {comp.type === 'console' && <boxGeometry args={[1, 0.04, 0.3]} />};      {comp.type === 'stair' && <boxGeometry args={[0.8, 1, 0.8]} />};      {comp.type === 'board' && <boxGeometry args={[1, 0.19, 0.02]} />};      {comp.type === 'net' && <boxGeometry args={[1, 1, 0.005]} />};      {comp.type === 'safety_net' && <boxGeometry args={[1, 1, 0.005]} />};      {comp.type === 'protection_roof' && <boxGeometry args={[1, 0.05, 1]} />};      {comp.type === 'load_plate' && <cylinderGeometry args={[0.15, 0.15, 0.04, 16]} />};      {isTransparent && (;        <meshStandardMaterial color={comp.color} transparent opacity={0.4} side={THREE.DoubleSide} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};      {isMetal && (;        <meshStandardMaterial color={comp.color} metalness={0.6} roughness={0.3} transparent opacity={opacity} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};      {isWood && (;        <meshStandardMaterial color={comp.color} metalness={0.0} roughness={0.8} transparent opacity={opacity} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};      {comp.type === 'deck' && (;        <meshStandardMaterial color={comp.color} metalness={0.3} roughness={0.7} transparent opacity={opacity} side={THREE.DoubleSide} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};      {comp.type === 'stair' && (;        <meshStandardMaterial color={comp.color} metalness={0.4} roughness={0.5} transparent opacity={opacity} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};      {(comp.type === 'footplate' || comp.type === 'load_plate') && (;        <meshStandardMaterial color={comp.color} metalness={0.8} roughness={0.2} transparent opacity={opacity} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};      {comp.type === 'protection_roof' && (;        <meshStandardMaterial color={comp.color} metalness={0.2} roughness={0.4} transparent opacity={0.7} side={THREE.DoubleSide} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};      {comp.type === 'coupling' && (;        <meshStandardMaterial color={comp.color} metalness={0.9} roughness={0.1} transparent opacity={opacity} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};      {comp.type === 'anchor' && (;        <meshStandardMaterial color={comp.color} metalness={0.8} roughness={0.2} transparent opacity={opacity} emissive={isSelected ? '#ffffff' : comp.color} emissiveIntensity={emissiveIntensity} />;      )};    </mesh>;  );;};function Building3D({ building, visible }: { building: CADModel['building']; visible: boolean }) {;  if (!visible) return null;;  const { lengthM, heightM, widthM, roofForm, roofHeightM } = building;;  const wallColor = '#e2e8f0';;  const roofColor = '#94a3b8';;  const w = widthM || 6;;  const roofH = roofHeightM || 0;;  return (;    <group position={[0, 0, -w / 2 - 0.5]}>;      <mesh position={[0, heightM / 2, 0]}>;        <boxGeometry args={[lengthM, heightM, w]} />;        <meshStandardMaterial color={wallColor} transparent opacity={0.6} />;      </mesh>;      {roofForm !== 'kein' && roofH > 0 && (;        <mesh position={[0, heightM + roofH / 2, 0]}>;          <coneGeometry args={[Math.max(lengthM, w) / 2 * 0.9, roofH, 4]} />;          <meshStandardMaterial color={roofColor} />;        </mesh>;      )};      {Array.from({ length: Math.min(building.windowCount, 10) }).map((_, i) => (;        <mesh;          key={`window-${i}`};          position={[-lengthM / 2 + (lengthM / (Math.min(building.windowCount, 10) + 1)) * (i + 1), heightM * 0.6, w / 2 + 0.01]};        >;          <boxGeometry args={[0.8, 1.0, 0.05]} />;          <meshStandardMaterial color={'#60a5fa'} transparent opacity={0.7} />;        </mesh>;      ))};      {Array.from({ length: Math.min(building.doorCount, 3) }).map((_, i) => (;        <mesh;          key={`door-${i}`};          position={[-lengthM / 2 + (lengthM / (Math.min(building.doorCount, 3) + 1)) * (i + 1), 1.0, w / 2 + 0.01]};        >;          <boxGeometry args={[1.0, 2.0, 0.05]} />;          <meshStandardMaterial color={'#78350f'} />;        </mesh>;      ))};    </group>;  );;};function DimensionLines({ model, visible }: { model: CADModel; visible: boolean }) {;  if (!visible) return null;;  const { building, totalHeightM } = model;;  const offset = 1.5;;  const color = '#f59e0b';;  return (;    <group>;      <MeasurementLine;        start={[-building.lengthM / 2 - offset, 0, 0]};        end={[-building.lengthM / 2 - offset, building.heightM, 0]};        label={`${building.heightM.toFixed(2)} m`};        color={color};      />;      <MeasurementLine;        start={[building.lengthM / 2 + offset, 0, 0]};        end={[building.lengthM / 2 + offset, totalHeightM, 0]};        label={`${totalHeightM.toFixed(2)} m`};        color={color};      />;      <MeasurementLine;        start={[-building.lengthM / 2, -offset, 0]};        end={[building.lengthM / 2, -offset, 0]};        label={`${building.lengthM.toFixed(2)} m`};        color={color};      />;      {model.fields[0] && (;        <MeasurementLine;          start={[model.fields[0].positionX - model.fields[0].lengthM / 2, -offset * 0.6, 0]};          end={[model.fields[0].positionX + model.fields[0].lengthM / 2, -offset * 0.6, 0]};          label={`${model.fields[0].lengthM.toFixed(2)} m`};          color={color};        />;      )};    </group>;  );;};function MeasurementLine({ start, end, label, color = '#f59e0b' }: {;  start: [number, number, number];;  end: [number, number, number];;  label: string;;  color?: string;;}) {;  const startVec = useMemo(() => new THREE.Vector3(...start), [start]);;  const endVec = useMemo(() => new THREE.Vector3(...end), [end]);;  const distance = useMemo(() => startVec.distanceTo(endVec), [startVec, endVec]);;  const midPoint = useMemo(() => new THREE.Vector3().addVectors(startVec, endVec).multiplyScalar(0.5), [startVec, endVec]);;  const direction = useMemo(() => new THREE.Vector3().subVectors(endVec, startVec).normalize(), [startVec, endVec]);;  const quaternion = useMemo(() => {;    const axis = new THREE.Vector3(0, 1, 0);;    return new THREE.Quaternion().setFromUnitVectors(axis, direction);;  }, [direction]);;  return (;    <group>;      <mesh position={[midPoint.x, midPoint.y, midPoint.z]} quaternion={quaternion}>;        <cylinderGeometry args={[0.015, 0.015, distance, 8]} />;        <meshStandardMaterial color={color} />;      </mesh>;      <Text position={[midPoint.x, midPoint.y + 0.3, midPoint.z]} fontSize={0.25} color={color} anchorX='center' anchorY='middle'>;        {label};      </Text>;    </group>;  );;};function CameraController({ viewMode, target }: { viewMode: string; target: [number, number, number] }) {;  const { camera } = useThree();;  useMemo(() => {;    const dist = 20;;    let pos: [number, number, number] = [dist, dist, dist];;    switch (viewMode) {;      case 'front': pos = [0, 5, dist]; break;;      case 'back': pos = [0, 5, -dist]; break;;      case 'left': pos = [-dist, 5, 0]; break;;      case 'right': pos = [dist, 5, 0]; break;;      case 'top': pos = [0, dist, 0]; break;;      case 'bottom': pos = [0, -dist, 0]; break;;      default: pos = [dist, dist, dist]; break;;    };    camera.position.set(...pos);;    camera.lookAt(target[0], target[1], target[2]);;  }, [viewMode, camera, target]);;  return null;;};function Scene({ model, showBuilding, showScaffold, showDimensions, selectedComponent, onSelectComponent, visibleTypes, viewMode }: Props) {;  const target: [number, number, number] = [0, model.building.heightM / 2, 0];;  return (;    <group>;      <CameraController viewMode={viewMode} target={target} />;      <ambientLight intensity={0.5} />;      <directionalLight position={[10, 10, 5]} intensity={1} castShadow />;      <pointLight position={[-10, 10, -10]} intensity={0.3} color='#3b82f6' />;      <Building3D building={model.building} visible={showBuilding} />;      {showScaffold && model.components3D.map((comp) => (;        <ComponentMesh;          key={comp.id};          comp={comp};          isSelected={selectedComponent === comp.id};          onClick={() => onSelectComponent(comp.id === selectedComponent ? null : comp.id)};          visible={visibleTypes[comp.type] !== false};        />;      ))};      <DimensionLines model={model} visible={showDimensions} />;      <mesh rotation={[-Math.PI / 2, 0, 0]} position={[0, -0.01, 0]}>;        <planeGeometry args={[100, 100]} />;        <meshStandardMaterial color='#1e293b' />;      </mesh>;    </group>;  );;};export default function Scaffold3D({ model, showBuilding, showScaffold, showDimensions, selectedComponent, onSelectComponent, visibleTypes, viewMode }: Props) {;  const cameraDistance = Math.max(model.building.lengthM, model.building.heightM) * 1.5 + 5;;  return (;    <div className='w-full h-full rounded-xl overflow-hidden border border-black/10 bg-[#fbfbfd] relative'>;      <Canvas shadows camera={{ position: [cameraDistance, cameraDistance / 2, cameraDistance], fov: 50 }}>;        <Scene;          model={model};          showBuilding={showBuilding};          showScaffold={showScaffold};          showDimensions={showDimensions};          selectedComponent={selectedComponent};          onSelectComponent={onSelectComponent};          visibleTypes={visibleTypes};          viewMode={viewMode};        />;        <Grid;          position={[0, -0.02, 0]};          args={[60, 60]};          cellSize={1};          cellThickness={0.5};          cellColor='#334155';          sectionSize={5};          sectionThickness={1};          sectionColor='#475569';          fadeDistance={50};          fadeStrength={1};          infiniteGrid;        />;        <OrbitControls;          enablePan;          enableZoom;          enableRotate;          minDistance={3};          maxDistance={100};          target={[0, model.building.heightM / 2, 0]};        />;      </Canvas>;      <div className='absolute bottom-4 left-4 bg-white/80 backdrop-blur rounded-xl px-3 py-2 text-xs text-[#424245] border border-black/10 pointer-events-none'>;        <p>🖱️ Links: Drehen | Rechts: Verschieben | Scroll: Zoomen</p>;      </div>;      {selectedComponent && (;        <div className='absolute top-4 right-4 bg-white/90 backdrop-blur rounded-xl px-4 py-3 text-sm border border-black/10 shadow-lg'>;          <p className='font-semibold text-[#1d1d1f]'>{model.components3D.find((c) => c.id === selectedComponent)?.name}</p>;          <p className='text-xs text-[#86868b] mt-1'>;            Art.-Nr.: {model.components3D.find((c) => c.id === selectedComponent)?.articleNumber};          </p>;          <p className='text-xs text-[#86868b]'>;            Typ: {model.components3D.find((c) => c.id === selectedComponent)?.type};          </p>;        </div>;      )};    </div>;  );;}